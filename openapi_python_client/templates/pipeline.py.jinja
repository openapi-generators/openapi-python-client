import dlt

from {{ package_name}}.source import {{ source_name }}


if __name__ == "__main__":
    pipeline = dlt.pipeline(pipeline_name="{{ source_name}}", destination='duckdb', dataset_name="{{ dataset_name }}", full_refresh=False)
    source = {{ source_name }}()
    info = pipeline.run(source)
    print(info)


