from http import HTTPStatus
from typing import Any, cast
from urllib.parse import quote

import httpx

from ...client import AuthenticatedClient, Client
from ...types import Response, UNSET, dump_dict__for_transport, dump_json__for_transport
from ... import errors

{% for relative in endpoint.relative_imports | sort %}
{{ relative }}
{% endfor %}

{% from "endpoint_macros.py.jinja" import header_params, cookie_params, query_params,
    arguments, client, kwargs, parse_response, raise_error_response, docstring, body_to_kwarg %}

{# Documented non-2xx responses are raised as errors.UnexpectedStatus, never returned, so they are not part of the
   parsed type. `success_response_type()` does that split on status code. #}
{% set parsed_type = endpoint.success_response_type() %}
{% set parsed_responses = (endpoint.responses | length > 0) and parsed_type != "Any" %}
{# With nothing to parse -- no declared responses, or a schema-less body such as the one
   FastAPI emits for `-> None` -- every branch of `_parse_response` either returns None or
   raises. `None` says that; `Any` would only suggest a value that never arrives. #}
{% set return_string = parsed_type if parsed_responses else "None" %}

def _get_kwargs(
    {{ arguments(endpoint, include_client=False) | indent(4) }}
) -> dict[str, Any]:
    {{ header_params(endpoint) | indent(4) }}

    {{ cookie_params(endpoint) | indent(4) }}

    {{ query_params(endpoint) | indent(4) }}

    _kwargs: dict[str, Any] = {
        "method": "{{ endpoint.method }}",
        {% if endpoint.path_parameters %}
        "url": "{{ endpoint.path }}".format(
        {%- for parameter in endpoint.path_parameters -%}
        {{parameter.python_name}}=quote(str({{parameter.python_name}}), safe=""),
        {%- endfor -%}
        ),
        {% else %}
        "url": "{{ endpoint.path }}",
        {% endif %}
        {% if endpoint.query_parameters %}
        "params": params,
        {% endif %}
        {% if endpoint.cookie_parameters %}
        "cookies": cookies,
        {% endif %}
    }

{% if endpoint.bodies | length > 1 %}
{% for body in endpoint.bodies %}
    if isinstance(body, {{body.prop.get_type_string(no_optional=True) }}):
        {{ body_to_kwarg(body) | indent(8) }}
        {%- if body.content_type == "multipart/form-data" %}
        headers["Content-Type"] = "multipart/form-data; boundary=+++"
        {% else %}
        headers["Content-Type"] = "{{ body.content_type }}"
        {% endif %}
{% endfor %}
{% elif endpoint.bodies | length == 1 %}
{% set body = endpoint.bodies[0] %}
    {{ body_to_kwarg(body) | indent(4) }}
    {%- if body.content_type == "multipart/form-data" %}
    headers["Content-Type"] = "multipart/form-data; boundary=+++"
    {% else %}
    headers["Content-Type"] = "{{ body.content_type }}"
    {% endif %}
{% endif %}

{% if endpoint.header_parameters or endpoint.bodies | length > 0 %}
    _kwargs["headers"] = headers
{% endif %}
    return _kwargs

{% if endpoint.is_jsonl_streaming %}
{# ===== Streaming JSONL endpoint ===== #}
{% set item_type = endpoint.jsonl_item_type() %}
{% import "property_templates/jsonl_property.py.jinja" as jsonl_template %}

async def stream(
    {{ arguments(endpoint) | indent(4) }}
) -> AsyncGenerator[{{ item_type }}, None]:
    {{ docstring(endpoint, "AsyncGenerator[" + item_type + ", None]", is_detailed=false) | indent(4) }}

    kwargs = _get_kwargs(
        {{ kwargs(endpoint, include_client=False) }}
    )

    async with client.get_async_httpx_client().stream(**kwargs) as response:
        {% for resp in endpoint.responses.patterns %}
        {% set code_range = resp.status_code.range %}
        {% if loop.first %}
        {% if code_range[0] == code_range[1] %}
        if response.status_code == {{ code_range[0] }}:
        {% else %}
        if {{ code_range[0] }} <= response.status_code <= {{ code_range[1] }}:
        {% endif %}
        {% else %}
        {% if code_range[0] == code_range[1] %}
        elif response.status_code == {{ code_range[0] }}:
        {% else %}
        elif {{ code_range[0] }} <= response.status_code <= {{ code_range[1] }}:
        {% endif %}
        {% endif %}
        {% if resp.prop.template == "jsonl_property.py.jinja" %}
            {{ jsonl_template.construct_async_stream(resp.prop) | indent(12) }}
        {% else %}
            {# Nothing to stream on this status, so raise -- unconditionally, unlike the non-streaming path. An async
               generator has no None to hand back, so honoring raise_on_unexpected_status here would silently yield an
               empty stream instead. Undocumented statuses still honor it, in the else below. #}
            await response.aread()
            {{ raise_error_response(resp) | indent(12) }}
        {% endif %}
        {% endfor %}
        else:
            await response.aread()
            if client.raise_on_unexpected_status:
                raise errors.UnexpectedStatus(response.status_code, response.content)

{% else %}
{# ===== Standard (non-streaming) endpoint ===== #}

{# `cast(..., None)` rather than widening the signature to `| None`: a caller that leaves
   raise_on_unexpected_status alone never reaches these returns, having an Optional in every call site sucks.
   With nothing to parse `return_string` is already the literal "None", where a cast is redundant. #}
{% macro suppressed_error_return() %}
{% if parsed_responses %}
return cast({{ return_string }}, None)
{% else %}
return None
{% endif %}
{% endmacro %}

def _parse_response(*, client: AuthenticatedClient | Client, response: httpx.Response) -> {{return_string}}:
    {% for response in endpoint.responses.patterns %}
    {% set code_range = response.status_code.range %}
    {% if code_range[0] == code_range[1] %}
    if response.status_code == {{ code_range[0] }}:
    {% else %}
    if {{ code_range[0] }} <= response.status_code <= {{ code_range[1] }}:
    {% endif %}
    {% if response.is_success() %}
        {{ parse_response(parsed_responses, response) | indent(8) }}
    {% else %}
        if client.raise_on_unexpected_status:
            {{ raise_error_response(response) | indent(12) }}
        {{ suppressed_error_return() | indent(8) }}
    {% endif %}
    {% endfor %}
    {% if endpoint.responses.default %}
    {{ parse_response(parsed_responses, endpoint.responses.default) | indent(4) }}
    {% else %}
    if client.raise_on_unexpected_status:
        raise errors.UnexpectedStatus(response.status_code, response.content)
    {{ suppressed_error_return() | indent(4) }}
    {% endif %}


def _build_response(*, client: AuthenticatedClient | Client, response: httpx.Response) -> Response[{{ return_string }}]:
    {% if not parsed_responses %}
    # Called for the status check alone -- it raises on an undocumented code and has no
    # value to hand back.
    _parse_response(client=client, response=response)
    {% endif %}
    return Response(
        status_code=HTTPStatus(response.status_code),
        content=response.content,
        headers=response.headers,
        {% if parsed_responses %}
        parsed=_parse_response(client=client, response=response),
        {% else %}
        parsed=None,
        {% endif %}
    )


async def _request_detailed(
    {{ arguments(endpoint) | indent(4) }}
) -> Response[{{ return_string }}]:
    {{ docstring(endpoint, return_string, is_detailed=true) | indent(4) }}

    kwargs = _get_kwargs(
        {{ kwargs(endpoint, include_client=False) }}
    )

    response = await client.get_async_httpx_client().request(
        **kwargs
    )

    return _build_response(client=client, response=response)

{# Always generated: `request` is the module's only public entry point, so gating it on
   having something to parse would leave an endpoint with no callable API at all. When
   there is no parsed body -- a 204, or FastAPI's `-> None` (an empty response schema) --
   it returns None and is called for its effect. #}
async def request (
    {{ arguments(endpoint) | indent(4) }}
) -> {{ return_string }}:
    {{ docstring(endpoint, return_string, is_detailed=false) | indent(4) }}

    {% if parsed_responses %}
    return (await _request_detailed(
        {{ kwargs(endpoint) }}
    )).parsed
    {% else %}
    await _request_detailed(
        {{ kwargs(endpoint) }}
    )
    {% endif %}

{% endif %}
